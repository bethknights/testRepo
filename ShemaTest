#Test
import random

random_numbers = [random.randint(1, 100) for _ in range(10)]
print("Random numbers:", random_numbers)
# This code generates a list of 10 random integers between 1 and 100
# and prints them to the console.